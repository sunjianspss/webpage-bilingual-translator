# 隐私政策

**网页双语翻译（Webpage Bilingual Translator）**
最后更新：2026-08-19，对应版本 v0.1.5

本扩展没有服务器，没有账号，也没有任何统计或遥测代码。它只在你主动点击
翻译时工作，把当前页面的正文发给**你自己指定的**翻译服务。开发者看不到
你的任何数据。

## 一、会发送出去的数据

只有在你点击「翻译此页面」或按下翻译快捷键之后，才会发生网络请求。

### 发送的内容

当前页面上**可见正文的纯文本**，按批次发送。每一批的形式是
`[编号, 文本]` 的列表。

### 不发送的内容

- 页面网址
- 页面标题
- Cookie、localStorage 或任何登录状态
- 表单内容、密码框内容
- 任何用户标识、设备标识或安装 ID

请求体里除了正文和一句翻译指令之外没有别的东西。

### 发送到哪里

取决于你在扩展里选择的翻译服务，只有两种可能：

| 选择 | 目的地 | 说明 |
|---|---|---|
| 本地 API | 你自己填写的地址，默认 `http://127.0.0.1:1234/v1` | 数据不离开你的电脑 |
| DeepSeek API | `https://api.deepseek.com` | 数据发给 DeepSeek，适用[其隐私政策](https://platform.deepseek.com/) |

**没有第三种可能。** 扩展代码里出现的外部地址只有上面这两个。

选择 DeepSeek 时，页面正文会离开你的电脑并交给第三方处理。请不要用它
翻译含有机密、个人敏感信息或受合规约束的页面——这种页面请使用本地 API。

## 二、保存在你浏览器里的数据

以下数据全部保存在**本机**的扩展存储中，不会上传到任何地方。

### 设置（`chrome.storage.local`）

包括 API 地址、模型名、目标语言、显示方式，以及**你填写的 API Key**。

API Key 以明文保存在扩展存储中。这适合个人使用；如果你的电脑由他人共用
或受管控，请注意这一点。API Key 只在扩展的后台使用，**不会进入任何网页
的运行环境**，网页上的脚本读不到它。

### 翻译缓存（`chrome.storage.local`）

为了让刷新页面不必重新翻译，译文会被缓存，上限约 3000 条，超出后淘汰
最早写入的。

- 缓存的**值**是译文原文，以明文保存。
- 缓存的**键**是页面地址（主机名 + 路径）的哈希，不是地址原文。需要说明
  的是，这里用的是普通的非加密哈希，目的是生成短小稳定的键名，**不构成
  匿名化保证**——掌握候选网址列表的人仍可比对出你访问过哪些页面。

### 翻译任务（`chrome.storage.session`）

翻译进行期间的任务状态，关闭浏览器即消失。

## 三、自动检测会连接你电脑上的端口

点击「自动检测」时，扩展会向**本机**发起请求，询问以下端口上是否有
OpenAI 兼容的模型服务在运行：

`1234`（LM Studio）、`11434`（Ollama）、`8080`（llama.cpp）、
`8000`（vLLM）、`1337`（Jan）

- 只在你**点击按钮**时发生，不会在后台自动进行。
- 只探测上面这 5 个写死的端口，**不会遍历端口范围**。
- 请求发往 `127.0.0.1`，不离开你的电脑。
- 只读取服务返回的模型列表，用于填写 API 地址和模型下拉，不做别的。

## 四、权限说明

| 权限 | 用途 |
|---|---|
| `activeTab` | 只在你点击扩展时，访问当前这一个标签页 |
| `scripting` | 把翻译脚本注入当前页面，用于提取正文和显示译文 |
| `storage` | 保存上面第二节列出的设置与缓存 |
| `http://127.0.0.1/*`、`http://localhost/*` | 连接你本机的模型服务 |
| `https://api.deepseek.com/*` | 选择 DeepSeek 时连接其接口 |
| `http://*/*`、`https://*/*`（可选） | 仅当你把本地 API 填成局域网内**其他机器**的地址时，才会向你请求授权。不填就永远不会请求 |

扩展**不会**在你未点击时读取任何页面，也不会在后台扫描浏览记录。

## 五、我们不做的事

- 不收集、不存储、不出售任何个人信息
- 没有任何统计、遥测、崩溃上报或广告 SDK
- 没有账号、登录或用户识别
- 不与除你所选翻译服务之外的任何第三方通信

## 六、删除你的数据

- **清除某一页的缓存**：在该页面打开扩展，点「清除缓存并重译」。
- **清除全部数据**：在 `chrome://extensions/` 中移除本扩展，其所有本机
  存储会随之删除。

## 七、儿童隐私

本扩展不面向 13 岁以下儿童，也不会有意收集其信息。

## 八、政策变更

本政策的历史版本可在本仓库的 Git 提交记录中查阅。涉及数据处理方式的
变更会同时记入 `CHANGELOG.md`。

## 九、联系方式

有疑问请通过本仓库的
[Issues](https://github.com/sunjianspss/webpage-bilingual-translator/issues)
提出。
